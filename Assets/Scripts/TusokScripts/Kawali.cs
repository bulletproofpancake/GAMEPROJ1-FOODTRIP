using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// kawali has ff:
/// - has capacity 
/// - can identify the contents inside kawali.
/// </summary>
public class Kawali : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
